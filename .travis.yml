language: php

php:
  - 5.4
  - 5.5
  - hhvm

matrix:
  allow_failures:
    - php: 5.5
    - php: hhvm

before_script:
  - mysql -e 'CREATE DATABASE titon_test;'
  - composer selfupdate
  - composer install

notifications:
  email: false